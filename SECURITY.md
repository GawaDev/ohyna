# セキュリティ方針

## 想定する提供形態

Ohyna はサーバに配置し、利用者がブラウザから利用します。

- アプリ本体（`python -m ohyna serve`）はリバースプロキシの背後に置く
- TLS（HTTPS）・認証・レート制限・アクセスログはプロキシまたは IdP で実施する
- Python プロセスをインターネットに直接公開しない
- 未認証のまま `0.0.0.0` 等で待ち受けない

開発時の既定バインドは `127.0.0.1` です。脅威モデルと HTTP 制約の詳細は [docs/spec/06-セキュリティ.md](./docs/spec/06-セキュリティ.md) を参照してください。

## 脆弱性の報告

セキュリティ上の問題を発見した場合は、公開の Issue に攻撃手順の詳細を書く前に、非公開で連絡してください。

優先順:

1. GitHub リポジトリの Security Advisory（Private vulnerability reporting）
2. リポジトリのメンテナ（GitHub プロフィールまたは README に記載の連絡先）

報告に含めてほしい情報:

- 影響を受ける版
- 再現手順
- 想定される影響範囲

## 運用上の要点

- 本文・PDF 生成 API は計算コストが高いため、プロキシでレート制限する
- CORS を広げる場合は環境変数 `OHYNA_ALLOWED_ORIGINS`（カンマ区切り）を使う（`*` は使わない）
