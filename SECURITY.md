# セキュリティ方針

## 想定する使い方

Ohyna は **サーバに展開し、一般利用者がブラウザ経由で利用する**ことを想定しています。

- アプリ本体（`python -m ohyna serve`）は **リバースプロキシの背後**に置く
- **TLS（HTTPS）・認証・レート制限・アクセスログ**はプロキシ／IdP 側で必ず実施する
- Python プロセスをインターネットへ直接晒さない
- 未認証のまま `0.0.0.0` 等で公開しない

開発時の既定バインドは `127.0.0.1` です。脅威モデルと HTTP 制約の詳細は [docs/spec/06-セキュリティ.md](./docs/spec/06-セキュリティ.md) を参照してください。

## 脆弱性の報告

セキュリティ上の問題を見つけた場合は、**公開の Issue に詳細な攻撃手順を載せる前に**私下で連絡してください。

優先順:

1. GitHub リポジトリの **Security Advisory**（Private vulnerability reporting）
2. リポジトリのメンテナ（GitHub プロフィール／README に記載の連絡先）

報告に含めてほしいもの:

- 影響を受ける版
- 再現手順
- 想定される影響範囲

## 運用者向けの要点

- 本文・PDF 生成 API は計算コストが高い。プロキシでレート制限する
- CORS を広げる場合は環境変数 `OHYNA_ALLOWED_ORIGINS`（カンマ区切り）のみを使う（`*` は不可）
